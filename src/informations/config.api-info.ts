/**
 * A string that represents the global prefix for API routes.
 * This prefix is commonly used to prepend or namespace all
 * API endpoints in an application, ensuring consistency and
 * organization in route definitions.
 */
export const globalPrefix = 'api';
